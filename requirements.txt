# ---- Core runtime (CPU) ----
tensorflow-cpu
keras

# ---- App/UI ----
streamlit

# ---- Data / utils ----
numpy
pandas
pillow

# ---- Compatibility pins (TF needs protobuf < 5) ----
protobuf

# ---- Only needed if you load .h5 checkpoints (safe to keep)
h5py

# --- VLM / CLIP path ---
torch
torchvision
open_clip_torch
joblib
scikit-learn

typing-extensions
